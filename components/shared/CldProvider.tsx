// components/shared/CldProvider.tsx
'use client';
import { CldUploadWidget } from 'next-cloudinary';

// This is a simple wrapper to make using the upload widget easier
// We will not use this directly, but it's a good practice to have.
// The main component will handle the logic.
export default CldUploadWidget;